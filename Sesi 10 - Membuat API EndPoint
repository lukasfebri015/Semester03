Berikut adalah contoh script JavaScript untuk membuat API Endpoint
untuk tabel mahasiswa dan dosen dengan masing-masing 10 data:

```javascript
// Import library dan framework yang diperlukan
const express = require('express');
const app = express();

// Data mahasiswa
const mahasiswa = [
  { nim: '110202020', nama: 'Lukas', gender: 'L', prod: 'I', alamat: 'Cisaat' },
  // Tambahkan data mahasiswa lainnya di sini
];

// Data dosen
const dosen = [
  { nip: '123456789', nama: 'John Doe', gender: 'L', jabatan: 'Dosen Tetap' },
  // Tambahkan data dosen lainnya di sini
];

// Endpoint untuk mendapatkan data mahasiswa
app.get('/mahasiswa', (req, res) => {
  res.json(mahasiswa);
});

// Endpoint untuk mendapatkan data dosen
app.get('/dosen', (req, res) => {
  res.json(dosen);
});

// Jalankan server pada port 3000
app.listen(3000, () => {
  console.log('Server berjalan pada port 3000');
});
```

Dalam contoh di atas, kita menggunakan framework Express untuk membuat server HTTP.
Data mahasiswa dan dosen disimpan dalam array. Kemudian, kita mendefinisikan
dua endpoint `/mahasiswa` dan `/dosen` yang akan mengembalikan data mahasiswa dan dosen secara JSON.

Untuk menjalankan script di atas, pastikan Anda telah menginstal Node.js dan Express.
Simpan script di dalam file dengan ekstensi `.js`, lalu jalankan dengan perintah `node nama_file.js`.
Setelah itu, Anda dapat mengakses data mahasiswa dan dosen melalui URL
`http://localhost:3000/mahasiswa` dan `http://localhost:3000/dosen`.
